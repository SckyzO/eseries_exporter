# Simple golangci-lint configuration for eseries_exporter
version: v1.24
run:
  timeout: 5m
  tests: true

linters:
  enable:
    - errcheck
    - gofmt
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - typecheck
    - unused
    - whitespace

severity:
  default: error
